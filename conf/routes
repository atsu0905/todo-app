# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

# An example controller showing a sample home page
GET     /                             controllers.HomeController.index
GET     /todo/list                    controllers.todo.TodoController.list
GET     /todo/store                   controllers.todo.TodoController.register
GET     /todo/$id<[0-9]+>             controllers.todo.TodoController.show(id: Long)
POST    /todo/store                controllers.todo.TodoController.store
GET     /category/list                controllers.todo.TodoController.list_category
GET     /category/$id<[0-9]+>         controllers.todo.TodoController.show_category(id: Long)
# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file                 controllers.Assets.versioned(path="/public", file: Asset)
